#!/usr/bin/env bash

set -e

if [ $# -lt 1 ]; then
  echo "Usage: gc \"<msg>\" [files...]" >&2
  exit 1
fi

msg="$1"
shift

if [ $# -eq 0 ]; then
  git commit -am "$msg"
else
  git add "$@" && git commit -m "$msg"
fi
